# you asked

message time: 2026-05-11 10:50:25

你帮我写一份详细的执行计划，里面不先写死具体方向，先写探索方向

---

# chatgpt response

下面这份计划的核心原则是：**先探索，不先假设用接口、Playwright、GenericAgent、Hermes 或 Claude Code。先把 OA 系统的真实可自动化边界摸清楚，再决定最终架构。**

# OA 合同审批自动审核 Agent 探索执行计划

## 一、目标

搭建一个自动化系统，定期检查办公 OA 中的待办流程；如果发现合同审批类待办，则自动获取合同附件，进行合同风险审核，并把审核意见发送到微信或企业微信。

当前阶段不直接确定技术路线，而是先完成以下探索：

1. OA 待办是否能通过接口读取；
2. 合同附件是否能通过接口下载；
3. 如果接口不可行，页面是否适合 Playwright 自动化；
4. 是否需要截图识别、OCR 或视觉模型；
5. 是否需要 Hermes / GenericAgent / Claude Code 参与长期运行；
6. 合同审核部分如何结构化、留痕、降低误判风险；
7. 微信通知和人工确认链路怎么设计最稳。

---

# 二、总体阶段划分

```text
阶段 1：环境和权限摸底
阶段 2：OA 待办数据获取方式探索
阶段 3：附件下载方式探索
阶段 4：合同审核流程探索
阶段 5：通知链路探索
阶段 6：运行架构对比
阶段 7：最小可用版本 MVP
阶段 8：稳定性、安全性、留痕增强
```

---

# 三、阶段 1：环境和权限摸底

## 1.1 收集基础信息

先记录 OA 系统的基本情况：

```text
OA 访问地址：
是否需要 VPN：
是否需要公司内网：
是否需要扫码登录：
是否需要短信/动态验证码：
是否会定期强制重新登录：
浏览器类型：
是否只能用特定浏览器：
合同附件格式：
待办页面入口：
合同审批流程名称关键词：
```

重点不是马上写代码，而是判断这个系统是否适合自动化。

## 1.2 判断登录复杂度

需要确认：

```text
是否有单点登录 SSO
是否需要企业微信扫码
是否需要手机验证码
是否绑定设备
是否检测自动化浏览器
是否长时间登录有效
```

判断结果分三类：

```text
A类：登录态稳定，可复用 Cookie
B类：登录态偶尔失效，需要人工重新登录
C类：强风控/强验证码，不适合全自动登录
```

如果是 C 类，不要硬做自动登录，应改成：**人工登录一次，自动化只复用已登录浏览器 Profile。**

---

# 四、阶段 2：OA 待办数据获取方式探索

这一阶段不先决定用接口还是 Playwright，而是同时探索三条路。

## 2.1 探索方向 A：接口方式

目标：找出 OA 待办列表是否有后端接口。

执行步骤：

1. 打开浏览器开发者工具；
2. 进入 Network；
3. 刷新 OA 待办页面；
4. 筛选 XHR / Fetch 请求；
5. 查找包含以下关键词的请求：

```text
todo
task
workflow
process
pending
approve
approval
待办
流程
审批
合同
```

重点观察：

```text
请求 URL
请求方法 GET / POST
请求参数
返回数据是否包含待办标题
返回数据是否包含流程 ID
返回数据是否包含附件信息
是否需要 Cookie
是否需要 token
token 来源在哪里
```

初步判断标准：

```text
如果接口返回 JSON 且包含待办列表 → 优先走接口路线
如果接口加密严重或签名复杂 → 暂缓接口路线
如果接口返回 HTML 片段 → 仍可解析，但稳定性中等
如果完全看不到有效接口 → 转 Playwright 页面路线
```

输出物：

```text
docs/oa-api-discovery.md
```

内容包括：

```text
发现的接口
请求参数
响应样例
是否可复现
是否可用于待办筛选
风险点
```

---

## 2.2 探索方向 B：Playwright DOM 自动化

目标：判断 OA 页面是否能被 Playwright 稳定识别和操作。

执行步骤：

1. 用 Playwright 打开 OA 页面；
2. 复用真实浏览器登录态；
3. 定位“待办流程”入口；
4. 读取待办列表；
5. 判断每一行是否包含合同审批关键词；
6. 点击合同待办详情。

优先使用这些定位方式：

```text
getByText()
getByRole()
locator('table tr')
locator('.class')
XPath
```

判断标准：

```text
如果待办列表是正常 HTML 表格 → Playwright 适配度高
如果按钮、链接、附件都能定位 → 可进入 MVP
如果页面大量 iframe → 复杂度上升，但仍可做
如果页面是 Canvas / 远程桌面 / 图片流 → DOM 方案不可行
```

输出物：

```text
docs/playwright-dom-feasibility.md
```

内容包括：

```text
待办页面 DOM 是否可读
列表是否可遍历
合同关键词是否可筛选
详情页是否可进入
附件按钮是否可定位
iframe 情况
弹窗情况
失败点
```

---

## 2.3 探索方向 C：截图 / OCR / 视觉模型

这个方向只作为兜底，不优先使用。

适用情况：

```text
页面 DOM 不可读
待办内容嵌在图片中
页面是 Canvas
远程桌面式 OA
按钮没有可识别元素
```

探索内容：

```text
截图能否稳定截取待办列表
OCR 能否识别流程标题
视觉模型能否判断合同待办
点击位置是否稳定
```

判断标准：

```text
如果页面布局固定，可以作为兜底
如果页面经常变化，不建议生产使用
如果必须依赖截图点击，需要人工确认机制
```

输出物：

```text
docs/vision-ocr-feasibility.md
```

---

# 五、阶段 3：附件下载方式探索

附件下载是关键，不要只看能不能点按钮，要确认下载后的文件是否完整、可识别、可追踪。

## 3.1 接口下载探索

如果在 Network 里能找到附件下载接口，需要记录：

```text
附件列表接口
附件 ID
文件名
下载 URL
是否需要 token
是否一次性链接
下载后文件是否损坏
是否能批量下载
```

判断标准：

```text
如果可以直接请求下载接口 → 最优
如果必须从页面点击生成下载链接 → 用 Playwright
如果下载链接一次性有效 → 运行时实时获取
```

## 3.2 Playwright 下载探索

执行步骤：

```text
进入合同详情页
定位附件区域
识别所有附件
筛选合同正文附件
触发下载
监听 download 事件
保存到本地目录
记录文件名和来源流程
```

目录建议：

```text
data/
  contracts/
    2026-05-11/
      OA流程ID_合同名称/
        原始附件/
        审核结果/
        日志.json
```

## 3.3 附件格式判断

需要支持：

```text
doc
docx
pdf
xls
xlsx
zip
ofd
图片扫描件
```

初期建议只支持：

```text
docx
pdf
xlsx
```

其他格式先标记为：

```text
需人工处理
```

输出物：

```text
docs/attachment-download-feasibility.md
```

---

# 六、阶段 4：合同审核流程探索

合同审核不能直接让模型自由发挥，应该结构化。

## 4.1 合同文本提取

探索不同格式的读取方式：

```text
docx：直接提取正文、表格、批注
pdf：优先文本层提取，扫描件再 OCR
xlsx：读取关键 sheet
zip：解压后递归识别
```

输出：

```text
合同正文文本
合同基本信息
关键条款位置
附件清单
提取失败说明
```

## 4.2 审核规则清单

先不要追求复杂，建立第一版规则：

```text
合同主体
合同金额
付款条款
发票条款
履约期限
违约责任
争议解决
解除条款
保密条款
知识产权
验收条款
担保/保证金
合同签署页
附件是否齐全
金额大小写是否一致
合同名称与审批标题是否一致
对方主体是否明确
```

## 4.3 审核结果格式

每份合同输出：

```text
合同名称：
OA流程编号：
附件名称：
审核时间：
风险等级：高 / 中 / 低 / 未发现明显风险

主要风险：
1.
2.
3.

建议审核意见：
……

需人工确认事项：
1.
2.

无法判断事项：
1.
2.
```

## 4.4 风险等级规则

不要让模型随意定级，先用规则约束：

```text
高风险：
- 合同主体不明
- 金额前后不一致
- 付款条件明显不利
- 缺少核心履约条款
- 争议解决异常
- 责任上限严重不利
- 合同附件缺失但正文强依赖附件

中风险：
- 表述不清
- 缺少验收标准
- 违约责任不对等
- 付款节点偏模糊
- 发票条款不完整

低风险：
- 文字瑕疵
- 格式问题
- 轻微表述优化
```

输出物：

```text
docs/contract-review-rules.md
src/review_contract.py
```

---

# 七、阶段 5：通知链路探索

不要一开始就强行发个人微信。优先顺序：

```text
企业微信机器人 > Server酱 / PushPlus > 邮件 > 个人微信机器人
```

## 5.1 企业微信机器人

探索内容：

```text
是否能创建群机器人
是否能发送 Markdown
是否能发送文件
是否能 @ 自己
是否公司允许
```

推荐通知格式：

```text
发现合同审批待办：3条

1. 合同名称：xxx
   风险等级：中
   主要风险：付款条款不清、验收标准缺失
   审核意见：建议补充……

2. 合同名称：xxx
   风险等级：低
   主要风险：未发现重大风险
   审核意见：可继续审批，建议完善……
```

## 5.2 个人微信通知

个人微信自动化风险较高，只作为备选。

探索方向：

```text
Server酱
PushPlus
微信文件传输助手自动化
企业微信个人通知
Bark
Telegram
邮件
```

判断标准：

```text
稳定性
是否容易封控
是否支持长文本
是否支持附件
是否有发送频率限制
```

输出物：

```text
docs/notification-feasibility.md
```

---

# 八、阶段 6：运行架构探索

这一阶段再判断 GenericAgent、Claude Code、Hermes、Playwright 各自的位置。

## 6.1 Claude Code 的角色

建议定位：

```text
开发者
调试者
维护者
代码生成者
日志分析者
异常修复助手
```

不建议定位：

```text
长期无人值守执行器
直接操作 OA 的生产机器人
自动提交审批意见的主体
```

## 6.2 Playwright 的角色

建议定位：

```text
浏览器自动化执行器
接口不可用时的页面操作工具
附件下载工具
```

适合做：

```text
打开 OA
读待办
点击详情
下载附件
保存文件
截图留证
```

不适合做：

```text
复杂法律判断
自由决策
自动审批
```

## 6.3 Hermes Agent 的角色

建议定位：

```text
定时任务入口
消息通知入口
后期多渠道指挥中心
```

适合做：

```text
每天定时触发脚本
收到通知后查询审核结果
把结果转发到 Telegram / 企业微信
```

不建议做：

```text
直接全权控制 OA
直接读取所有本地文件
直接修改审批意见
```

## 6.4 GenericAgent 的角色

建议定位：

```text
实验性备选
探索性自动化工具
应急处理复杂页面的研究方向
```

不建议作为主生产方案，原因：

```text
权限过宽
行为不够固定
审计留痕弱
合同场景风险高
```

---

# 九、阶段 7：最小可用版本 MVP

MVP 不追求全自动提交，只做：

```text
定时检查 OA
识别合同待办
下载附件
生成审核意见
发通知
人工确认
```

## MVP 功能清单

```text
1. 手动启动脚本
2. 复用已登录浏览器
3. 读取待办列表
4. 筛选合同审批
5. 下载附件
6. 提取合同文本
7. 调用合同审核模块
8. 生成 Markdown 审核报告
9. 发送企业微信/微信通知
10. 保存运行日志
```

## 不进入 MVP 的功能

```text
自动登录 OA
自动提交审批意见
自动点击同意/驳回
复杂多轮 Agent 自主判断
长期自我进化
自动改写审核规则
```

---

# 十、阶段 8：安全和留痕要求

## 8.1 权限控制

```text
只读 OA 待办
只下载合同附件
不自动提交审批
不自动删除文件
不自动修改 OA 数据
```

## 8.2 文件隔离

```text
合同文件单独目录
日志单独目录
审核结果单独目录
敏感文件不上传到不可信服务
```

## 8.3 日志字段

每次运行记录：

```text
运行时间
OA账号
待办数量
合同待办数量
流程编号
合同名称
附件名称
下载状态
审核状态
通知状态
错误信息
截图路径
```

## 8.4 异常处理

遇到以下情况应停止并通知人工：

```text
登录失效
验证码
待办页面结构变化
附件下载失败
合同无法读取
审核模型报错
发现高风险合同
发现多个同名附件
附件格式不支持
```

---

# 十一、最终探索输出物

Claude Code 应该先产出这些文件，而不是直接写死最终方案：

```text
docs/
  01-oa-environment.md
  02-oa-api-discovery.md
  03-playwright-feasibility.md
  04-attachment-download-feasibility.md
  05-contract-review-rules.md
  06-notification-feasibility.md
  07-architecture-comparison.md
  08-mvp-plan.md

src/
  discover_oa_api.py
  test_playwright_login.py
  test_todo_list.py
  test_download_attachment.py
  extract_contract_text.py
  review_contract.py
  send_notification.py

data/
  contracts/
  logs/
  reports/
```

---

# 十二、给 Claude Code 的执行指令

可以直接发给 Claude Code：

```text
你现在不要直接写死最终实现方案。

我的目标是搭建一个自动化系统：定期查看办公 OA 的待办流程，如果发现合同审批类待办，就自动下载合同附件，进行合同审核，并把审核意见发送到微信或企业微信。

请先做探索阶段，不要一开始就假设必须使用 Playwright、接口、Hermes、GenericAgent 或其他方案。

请按以下顺序执行：

1. 先建立项目目录和 docs 文档结构；
2. 先设计探索方案，不要直接进入生产开发；
3. 探索 OA 待办是否可以通过接口获取；
4. 探索附件是否可以通过接口下载；
5. 如果接口不可行，再探索 Playwright 是否能稳定操作页面；
6. 判断页面是否需要截图、OCR 或视觉模型兜底；
7. 探索合同附件格式和文本提取方式；
8. 设计合同审核规则清单；
9. 探索通知方式，优先企业微信机器人，其次 Server酱 / PushPlus / 邮件，不优先使用个人微信机器人；
10. 对比 Claude Code、Playwright、Hermes Agent、GenericAgent 在本项目中的合理角色；
11. 最后输出一份架构建议，不要提前下结论。

限制条件：

- 不要自动提交 OA 审批意见；
- 不要自动点击同意或驳回；
- 不要保存明文密码；
- 不要绕过验证码；
- 不要扩大本地文件读取权限；
- 所有运行过程必须留日志；
- 所有异常必须停止并通知人工；
- 合同全文不要直接发送到微信，只发送摘要、风险点和审核建议；
- 高风险合同必须标记为人工确认。

最终交付：

docs/01-oa-environment.md
docs/02-oa-api-discovery.md
docs/03-playwright-feasibility.md
docs/04-attachment-download-feasibility.md
docs/05-contract-review-rules.md
docs/06-notification-feasibility.md
docs/07-architecture-comparison.md
docs/08-mvp-plan.md

并在完成探索后，给出推荐路线：
A. 接口优先方案
B. Playwright 方案
C. Hermes 调度方案
D. GenericAgent 实验方案
E. 混合方案

不要直接进入全自动生产系统，先完成可行性验证。
```